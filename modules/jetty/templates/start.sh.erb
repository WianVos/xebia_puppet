#!/bin/sh

export JAVA=<%= java_home %>/bin/java
export JETTY_HOME=<%= installdir %>
export JETTY_RUN=$JETTY_HOME

# Start the jetty server ...
/bin/su  <%= runtime_user %> -c '<%= installdir %>/bin/jetty.sh start' > /dev/null 2>&1

exit $?