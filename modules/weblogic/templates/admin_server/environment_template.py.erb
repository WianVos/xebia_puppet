# WebLogic properties template, replace everything between '<>' angle brackets
scheme='<dev|sys|tst|acc|prd>'

# domain settings
weblogic_password='<weblogic_password>'
domain_name='<domain_name>'

# Listen addresses, if this isn't a 2 node setup, modify the domain topology below
list_of_servers=['<server_address_admin>', '<server_address_1>', '<server_address_2>']

#wl_cluster1={'FrontendHost':'<vip_address>'}

###############################################################################
# Domain Topology
###############################################################################

# Clusters
clusters=['wl_cluster1']

# Machines
machines=['wl_machine1','wl_machine2']
wl_machine1={'ListenAddress':list_of_servers[1]}
wl_machine2={'ListenAddress':list_of_servers[2]}

# Admin Server 
admin_server={'ListenAddress':list_of_servers[0]}
admin_server_machine={'ListenAddress':list_of_servers[0]}

# Managed Servers
managed_servers=['wl_server1','wl_server2']
wl_server1={'ListenAddress':list_of_servers[1]}
wl_server1_cluster_broadcast_channel={'ListenAddress':list_of_servers[1]}
wl_server2={'ListenAddress':list_of_servers[2]}
wl_server2_cluster_broadcast_channel={'ListenAddress':list_of_servers[2]}